# ─── Production ───────────────────────────────────────────────────────────────
# Vite bakes these values into the bundle at build time.
# Set VITE_API_BASE_URL to your deployed backend URL.
# Leave blank if the frontend is served from the same origin as the API
# (e.g., nginx proxies /api → backend), in which case relative /api paths work.
VITE_API_BASE_URL=https://3.109.75.176
VITE_FRONTEND_URL=https://d2fotbn4kted35.cloudfront.net
